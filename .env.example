# .env.example
# Copy to .env and fill in your keys

# Required for LLM generation (Groq free tier: https://console.groq.com/keys)
GROQ_API_KEY=your_groq_api_key_here

# Optional: For higher rate limits on DDG (not required)
# DDG_RATE_LIMIT_DELAY=1.5